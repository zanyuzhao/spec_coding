# Global Guard

## 强制

1. **类型**：全仓库禁止使用 `Any` / `any`。用具体类型、泛型或 `unknown`（再收窄）。
2. **自然语言**：与用户、注释、文档、提交说明等用**中文**交流。
3. **代码与标识符**：代码（变量、函数、类、文件名、接口路径、配置键等）一律使用**英文**。

## 适用

- 所有 `backend/**`、`frontend/**`、脚本与配置。`docs/spec` 内业务描述可用中文。
